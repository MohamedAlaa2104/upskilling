<IfModule mod_rewrite.c>
    RewriteEngine On

    # Rewrite URLs that are not files or directories to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]

</IfModule>
